"""Structural (schema-level) data model components, partitioned by source type.

Submodules
----------
rdbms
    Relational structural components (Database, Schema, Table, Column).
lpg
    Labeled Property Graph structural components (Database, Schema, Node,
    Relationship, Property). In-progress; importing it raises a warning.
"""
