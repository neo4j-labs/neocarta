"""Offline drivers for real connector extractors, shared across test suites.

Support-only: uncollected by pytest and outside ``[tool.coverage.run] source``, so nothing here
moves a production measurement.
"""
